[project]
name = "fitfi-recommender-langgraph"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.2.0",
    "pydantic>=2.13.4",
]
